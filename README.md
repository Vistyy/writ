# Instruction lint

Local Markdown linting for the agent instructions owned by this repository.

The configured scope includes root agent instructions, agent definitions, prompt templates, skills and their references, and user skills. No lint rules are enabled yet.

```sh
pnpm install --frozen-lockfile
pnpm check
```
